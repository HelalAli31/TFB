import { FakeImageDirective } from './fake-image.directive';

describe('FakeImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FakeImageDirective();
    expect(directive).toBeTruthy();
  });
});
